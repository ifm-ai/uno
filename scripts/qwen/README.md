# Qwen evaluation scripts

This directory contains the generic Uno benchmark runner and the default Qwen3
release configuration. Override `MODEL`, `GATED_LORA_PATH`, and the sampling
environment variables to evaluate a different compatible checkpoint.
